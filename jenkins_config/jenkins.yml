jenkins:
  agentProtocols:
  - "JNLP4-connect"
  - "Ping"
  clouds:
  - docker:
      dockerApi:
        connectTimeout: 60
        dockerHost:
          uri: "unix:///var/run/docker.sock"
        readTimeout: 60
      name: "docker"
  labelString: "docker"
  slaveAgentPort: 50000
  updateCenter:
    sites:
    - id: "default"
      url: "https://updates.jenkins-ci.org/update-center.json"
  location:
    url: "http://192.168.33.5/"
  pipeline-model-docker:
    dockerLabel: "docker"
tool:
  ansibleInstallation:
    installations:
    - name: "ansible-2.9"
      properties:
      - installSource:
          installers:
          - command:
              command: |-
                python -m virtualenv ansible-2.9/
                ansible-2.9/bin/pip install ansible==2.9.1
              toolHome: "ansible-2.9/bin"
  dockerTool:
    installations:
    - name: "Default"
      properties:
      - installSource:
          installers:
          - docker:
              version: "latest"
  git:
    installations:
    - home: "git"
      name: "Default"
